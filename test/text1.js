const cart = {
    id: string, 
    userId: string,
    products: [{
        id: string,
        title: string,
        price: number,
        quantity: number, 
        image: string, 
        description: string
    }]
}